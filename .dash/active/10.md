# GH-10: Improve /refine task granularity constraints

## Spec
- Update the task generation prompt in `refine.md` to require each task produce a verifiable outcome
- Change applies to both `.claude/commands/refine.md` and the copy written by `_dash_install_commands` in `dash.sh`
- Single-line addition to the Tasks instruction: "Each task should produce a verifiable outcome (a passing test, a working command, a visible change)."

## Tasks
- [ ] Update Tasks instruction in `.claude/commands/refine.md`
- [ ] Update the matching heredoc in `_dash_install_commands` in `dash.sh`

## Log
- 26-03-11: Created from issue
