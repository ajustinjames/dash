# GH-4: Archive active files on dash done instead of deleting

## Spec
- On `dash done`, move `.dash/active/{issue}.md` to `.dash/archive/{issue}.md` instead of deleting
- Create `.dash/archive/` dir during `dash init` (and lazily if missing on done)
- Preserves spec, tasks, and log notes for retrospectives; IC entry in history.log remains unchanged

## Tasks
- [ ] Create `.dash/archive/` in `_dash_init`
- [ ] Update `_dash_done` to `mv` active file to archive instead of `rm`
- [ ] Guard: create archive dir lazily in `_dash_done` if it doesn't exist
- [ ] Update `dash init` hook installation to also create archive dir on re-init
- [ ] Update README to mention `.dash/archive/`
- [ ] Update CLAUDE.md structure section to include `archive/`

## Log
- 26-03-11: Created from issue
