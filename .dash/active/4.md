# GH-4: Archive active files on dash done instead of deleting

## Spec
- On `dash done`, move `.dash/active/{issue}.md` to `.dash/archive/{issue}.md` instead of deleting
- Create `.dash/archive/` dir during `dash init` (and lazily if missing on done)
- Preserves spec, tasks, and log notes for retrospectives; IC entry in history.log remains unchanged

## Tasks
- [x] Create `.dash/archive/` in `_dash_init`
- [x] Update `_dash_done` to `mv` active file to archive instead of `rm`
- [x] Guard: create archive dir lazily in `_dash_done` if it doesn't exist
- [x] Update `dash init` hook installation to also create archive dir on re-init
- [x] Update README to mention `.dash/archive/`
- [x] Update CLAUDE.md structure section to include `archive/`

## Log
- 26-03-11: Created from issue
- 26-03-11: Implemented
